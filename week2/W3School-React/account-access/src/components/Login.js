import React from 'react'; 

Login = () => {

renderApp = () => { 

    <h1>"Welcome!"</h1>
}

renderLogin = () => {

    <h2>Please log in</h2>
}

// Does the rule: Write JavaScript outside of JSX apply here?

    let authenticated = true;

return (

if (authenticated) {

    renderApp();

} else {

    renderLogin();
}

);

}


export default Login;